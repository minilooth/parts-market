import React from 'react';

import {SettingsTab} from '@components/settings/tabs/wrapper/SettingsTab';

export const CitiesTab: React.FC = () => {
  return (
    <SettingsTab title="Cities">
      Content
    </SettingsTab>
  )
}